# 探索レイヤ設計 — ちゃっぴー案のレビューと合体版

ChatGPT 案（Grok → Gemini → GPT の3段リサーチ）をレビューし、既存の制作パイプラインと
統合した設計。**採用した部分・変えた部分・その理由**を明示する。

---

## 1. レビュー

### 採用した部分（そのまま or ほぼそのまま）

| 項目 | 評価 |
|---|---|
| **「まだ競合が少ないのに伸び始めている兆候」を狙う** という中核思想 | ここが一番良い。既存パイプラインはニッチ固定だったので、この探索レイヤが欠けていた |
| 100点満点の評価軸（需要20/競合15/収益性20/成長15/コンテンツ化10/アフィリ10/継続5/信頼5） | 配点まで妥当。**そのまま採用**（`src/scout/models.py` の `RUBRIC`） |
| now / watch / drop の3段判定 | 判断が一発で決まるので採用 |
| 「今日の1位」の出力フォーマット | スマホで読む形として良い。**そのまま採用**（`src/scout/report.py`） |
| 上位だけ提示（大量に見せない） | 採用。見なくなったら終わりなので正しい |
| MVP から始めて段階的に足す | 採用 |

### 変えた部分（3点）

#### ① 3社の API を使う理由がない → **2社に減らした**

役割分担は綺麗に見えるが、実際にやっているのは (a) SNS検索 (b) Web検索 (c) 統合評価の3つで、
**(b) と (c) は 1 モデルで両方できる**。3社に分けると、キー・課金・レート制限・SDK差分・
障害点が3倍になる。MVP でこれは自殺行為。

| ちゃっぴー案 | 合体版 | 理由 |
|---|---|---|
| Grok: SNS発掘 | **Grok（維持）+ X API（追加）** | X 上の空気を読む部分は他モデルで代替しにくい。ただし Grok の「伸びている」判断は LLM の主観なので、X API から**いいね/時間を実測**して併用する |
| Gemini: Web調査 | **Claude の `web_search` サーバーツール** | 検索・読解・構造化が1リクエストで完結する。3社目を保守する価値がない |
| GPT: 統合・評価 | **Claude（structured outputs）** | JSON スキーマで出力を固定できるので、パース崩れで承認キューが汚れない |

Grok を残したのは温存ではなく、**X の本文アクセスが本当に代替しにくい**から。
無効化しておけば X API だけで動くし、両方無くてもサンプルで全体が回る。

#### ② スコアが出ることと儲かることは別 → **主観と実測を分離した**

ちゃっぴー案の致命的な弱点。**LLM は「競合の少なさ」を検索せずに推測で答えられてしまう。**
自信満々に「競合が少ないです、15点」と書く。これはただの占いで、意思決定に使えない。

合体版は 2 段構えにした（`src/scout/scoring.py`）。

1. **LLM 採点（主観）** — 評価軸どおりに 100 点満点で採点させる
2. **実測シグナルで補正（客観）**

| 実測 | 補正 |
|---|---|
| 検索で見えた独立ドメインが閾値以上なのに「競合が少ない」と採点 | **-10** ＋ 矛盾フラグ |
| 根拠 URL が 0 件 | **-8** ＋ 矛盾フラグ |
| いいね/時間が閾値未満（X 発掘） | **-5**（「伸び始めている」の否定） |
| いいね/時間が閾値の4倍以上 | **+5** |
| 何度も観測されているのに伸びていない | **-5**（継続性ではなく陳腐化） |
| 過去に実際に収益が出たキーワード | **+5**（成果フィードバック） |

矛盾（`conflicts`）はレポートに `⚠️ LLMと実測の食い違い` として出る。
**LLM を疑える形で人間に渡すこと**が、この設計の主目的。

さらに、順位付けは合計点ではなく **`early_signal = 成長性 × 競合の少なさ`** を主軸にした。
合計点で並べると「すでに大流行しているテーマ」（需要20・成長15・競合1）が上位に来て、
システムの目的と真逆になる。テストで固定してある
（`tests/test_scout_scoring.py::test_順位付けは合計点より早期シグナルを優先する`）。

#### ③ 最大の欠陥: 発掘しても何も作られない → **制作パイプラインに直結した**

ちゃっぴー案は「毎日 TOP3 が出てくる」で終わっている。
**情報収集システムはこれで必ず死ぬ。** 3日で見なくなって終わる。

合体版は `/adopt <id>` を出口に置いた。

```
探索レイヤ  発掘 → 裏取り → 採点 → 日次レポート Issue
                                          │
                        通勤中に  /adopt <id>
                                          ▼
                          data/adopted_niches.yaml
                                          │
制作レイヤ  収集(このクエリで) → 台本/記事 → 承認Issue → /approve → 投稿
                                          ▼
                              収益ログ (data/revenue.csv)
                                          │
                        `/revenue 3200 A8 インボイス`
                                          ▼
                          探索レイヤのスコアに +5 で還る
```

これで **リサーチ → 制作 → 投稿 → 成果 → スコア** が閉じる。
人間の判断は 2 種類だけになる。

- **週1〜数日に1回**: `/adopt` — どのニッチを攻めるか
- **毎日3〜5分**: `/approve` — 出てきた原稿を出すか

### その他の指摘

| 指摘 | 対応 |
|---|---|
| DB / Notion / Sheets は MVP には過剰 | **JSONL + git** にした。履歴と監査ログが無料で付き、Actions のランナーが破棄されても状態が残る |
| Discord / Slack / Telegram 通知 | 不要。GitHub Issue が既に通知面として機能している（アプリのプッシュ通知が来る） |
| 「過去ネタとの比較」を後回しにしている | **MVP に入れた。** 後回しにすると絶対入らないし、同じネタを毎日出すシステムは即座に見捨てられる |
| 重複は「捨てる」ではなく統合すべき | 3日連続で出て**伸びている**ネタは初出より有望。観測回数と伸びを追跡し、伸びなければ減点する形にした |
| 収益化候補に「情報商材」が入っている | 外した。既存 PLAYBOOK で地雷に分類している（ステマ規制・特商法・返金トラブル） |
| YMYL への言及がない | リスク項目に含めるようプロンプトに明記。お金ジャンルは検索評価が厳しい |
| Grok のモデル名 | 変わるので `config.yaml` で指定する形にした。既定値は動かない可能性がある（[docs.x.ai](https://docs.x.ai/) で確認） |

---

## 2. システム構成

```
                    ┌─────────── 探索レイヤ (src/scout/) ───────────┐
                    │                                               │
  X API v2 ────────▶│ sources/x_api.py    いいね/時間を実測          │
  Grok (xAI) ──────▶│ sources/grok.py     X上の兆候（任意・無効可）  │
                    │            │                                  │
                    │            ▼                                  │
                    │ store.py   重複統合＋観測回数の追跡            │
                    │            ▼                                  │
  Claude ──────────▶│ research.py  web_search で裏取り＋実測         │
  (web_search)      │            ▼                                  │
  Claude ──────────▶│ scoring.py   100点採点 → 実測で補正 → 判定     │
  (structured out)  │            ▼                                  │
                    │ report.py   「今日の1位」                      │
                    └──────────────────┬────────────────────────────┘
                                       │ レポート Issue
                            📱 /adopt <id>
                                       ▼
                        data/adopted_niches.yaml
                                       │
                    ┌──────────────────▼── 制作レイヤ (src/) ────────┐
                    │ collectors → processing → monetize → video    │
                    │            → 承認Issue → /approve → 投稿      │
                    └──────────────────┬────────────────────────────┘
                                       ▼
                        data/posts.csv, data/revenue.csv
                                       │ `/revenue`
                                       └──▶ 探索レイヤのスコアへ還元
```

## 3. 必要な API

| API | 必須 | 用途 | 未設定時 |
|---|---|---|---|
| **Anthropic (Claude)** | ◎ | 裏取り（web_search）・採点・台本・記事 | 候補一覧のみ出力、`未採点`と明示 |
| X API v2 | ○ | 需要の兆候収集、いいね/時間の実測 | サンプル候補で動作 |
| xAI (Grok) | △ | X 上の兆候発掘 | スキップ（`scout.grok.enabled: false` が既定） |
| ~~Gemini~~ | ✗ | **不要**（Claude の web_search で代替） | — |
| ~~OpenAI~~ | ✗ | **不要**（Claude で代替） | — |
| TikTok Content Posting | △ | 自動投稿 | `DRY_RUN` でファイル出力のみ |

`ANTHROPIC_API_KEY` 1 つで探索から制作まで通る。

## 4. データ構造

`data/scout/opportunities.jsonl` — 1行1機会。

```jsonc
{
  "id": "f2af5ac317",                    // キーワード集合のハッシュ（翌日も同一判定できる）
  "candidate": {                          // 発掘段階の生ネタ
    "title": "...", "summary": "...", "source": "x_api",
    "keywords": ["インボイス", "会計ソフト"],
    "evidence_urls": ["https://..."],
    "signals": {"likes_per_hour": 52.0}   // 機械計測。総いいね数より重要
  },
  "research": {                           // 裏取り結果
    "why_now": "...", "jp_demand": "...", "overseas_lead": "...",
    "competitor_note": "...", "target_user": "...",
    "monetization_paths": ["アフィリ記事", "有料note"],
    "best_product": "...", "risks": ["..."], "sources": ["https://..."],
    "measured": {"competitor_domains": 3, "evidence_count": 7}   // LLMの自己申告ではない
  },
  "score": {                              // 100点の内訳 + 実測補正
    "demand": 18, "low_competition": 13, "monetizability": 16, "trend_growth": 14,
    "contentability": 8, "affiliate_fit": 8, "durability": 4, "source_reliability": 4,
    "llm_verdict": "now", "scored": true,
    "machine_adjust": -8,
    "adjust_reasons": ["根拠URLが0件で -8"],
    "conflicts": ["LLMは競合が少ないと判断だが独立ドメインが12件"],
    "llm_total": 85, "total": 77, "early_signal": 0.809
  },
  "verdict": "now",                       // LLMと機械判定の保守側
  "action": "比較記事を1本書く",
  "first_seen": "...", "last_seen": "...", "times_seen": 3,
  "status": "adopted"                     // new | adopted | dropped
}
```

`data/adopted_niches.yaml` — 探索と制作の接点。

```yaml
niches:
  - slug: adopted_f2af5ac317        # 制作レイヤのカテゴリ名になる
    label: インボイス2割特例の終了で…
    query: (インボイス OR 2割特例 OR 会計ソフト) lang:ja -is:retweet
    opportunity_id: f2af5ac317
    best_product: 比較記事
    active: true
```

## 5. MVP 実装手順（実施済み）

ちゃっぴー案の MVP 定義（発掘 → 評価 → TOP3 → 保存）に沿ったが、
③の理由で **`/adopt` までを MVP に含めた**（出口が無いと価値が出ないため）。

1. ✅ データモデル（`models.py`）— 評価軸・early_signal・トークン正規化
2. ✅ 発掘元アダプタ（`sources/`）— X API（実測付き）+ Grok（任意）
3. ✅ 裏取り（`research.py`）— Claude web_search + 実測ドメイン数
4. ✅ 採点（`scoring.py`）— 100点採点 + 実測補正 + 保守側判定
5. ✅ 永続化と重複統合（`store.py`）— JSONL、観測回数の追跡
6. ✅ レポート（`report.py`）—「今日の1位」+ 矛盾の明示
7. ✅ ニッチ採用（`niches.py`）— 制作レイヤへの接続
8. ✅ CLI（`python -m src.pipeline scout`）と Actions（`daily-scout.yml`）
9. ✅ テスト 27 件

### 次に足すもの（優先順）

| 優先 | 項目 | 理由 |
|---|---|---|
| 高 | **検索ボリュームの実測** | 現状「競合の少なさ」は検索結果のドメイン数が代理指標。実際の検索需要は測れていない |
| 高 | **採用ニッチ専用のアフィリ案件** | いまは `offers.default` に落ちる。ニッチごとに単価の高い案件を割り当てたい |
| 中 | 既存記事の質の評価 | ドメイン数だけでは「競合が多いが全部薄い」を見分けられない |
| 中 | 採用ニッチの自動休止 | 30日で収益0のニッチは自動で `active: false` にする |
| 低 | Gemini / GPT の追加 | 現状の構成で困ってから。困っていないうちに増やすと保守が破綻する |

## 6. 使い方

```bash
# 探索（サンプル候補、APIキー不要）
python -m src.pipeline scout --sample

# 本番（ANTHROPIC_API_KEY + X_BEARER_TOKEN）
python -m src.pipeline scout --open-issue

# 採用 → 制作へ
python -m src.pipeline command --body "/adopt f2af5ac317"
python -m src.pipeline run --limit 3        # 採用ニッチが自動でクエリに入る

# 成果を戻す（探索レイヤのスコアに +5 で反映される）
python -m src.pipeline revenue --amount 3200 --source A8 --note インボイス
```

## 7. コスト感

`research_limit: 6`（既定）で 1 日あたり Claude API 呼び出しは
**裏取り6回（web_search 付き）+ 採点6回 = 12回**。
制作レイヤの台本・記事生成が別途 1 本あたり 2 回。

`config.yaml` の `scout.research_limit` と `llm.effort` が主なコスト調整点。
API 代を下げたいときは `research_limit` を先に絞る（採点より検索の方が高い）。

---

# 第2ラウンド — GPT の改訂案へのレビューと実装

GPT が改訂案（Evidence 層 / Discovery・Business 分離 / SERP Weakness / 検索需要実測 /
ファネル別撤退 / Experiment Ledger / explore-exploit / 条件付き Gemini）を出してきた。
実装者として、**費用対効果が悪い部分**と**今MVPに絶対入れる部分**を分けて判定した。

判定基準は1つだけ。**最短で最初の売上に効くか。**

## 判定表

| # | GPT提案 | 判定 | 理由 |
|---|---|---|---|
| ② | observed が inferred を**置き換える**（足し引きしない） | ✅ **MVP実装済** | 正しい。旧実装の -10 は汚いハックだった。新規APIゼロ |
| ③ | Discovery / Business / Opportunity = √(D×B) | ✅ **MVP実装済** | 正しい。「入る余地はあるが金にならない」を排除できる。新規APIゼロ |
| ⑧ | 収益+5 を廃止し explore/exploit 予算に | ✅ **MVP実装済** | 指摘どおりループになる。新規APIゼロ |
| ⑨ | Experiment Ledger（予測の凍結） | ✅ **MVP実装済** | 記録だけ。分析機構は作らない |
| ⑦ | ファネル別撤退（時間ではなく試行回数） | ✅ **MVP実装済**（手入力） | 30日基準より正しい。ただし指標取得は API 審査が重いので `/metrics` 手入力から |
| ⑩ | 目的関数 = 人間の注意1単位あたり期待収益 | 🟡 **フレームのみ採用** | EV式は却下（下記）。分母（判断時間）だけ実測して可視化 |
| ④ | SERP Weakness Score | 🟡 **概念採用・データ源は代理** | 考え方は正しい。実SERPは有料APIなので無料の代理指標＋低confidenceで入れた |
| ⑤ | 検索需要の実測（Google Ads API） | ❌ **初売上まで延期** | 下記。**優先度1位に置くのは反対** |
| ① | 条件付き Gemini クロスチェック | ❌ **延期（おそらく不要）** | 下記 |
| ⑥ | 配点は変えず実績に決めさせる | ✅ **完全同意** | 素点100 / Discovery / Business を並列保存済み |

## 反対した3点の理由

### ⑤ Google Ads API を優先度1位にするのは反対

**Google Ads API のキーワード検索数取得には、承認済みデベロッパートークンが必要。**
Basic access は通常、稼働中の広告アカウント（＝出稿実績）が前提になる。
つまり **1円も稼いでいない段階で、審査待ちと広告費が先に発生する。**
Google Trends 公式 API も 2026年8月時点で Alpha・限定アクセス（GPT自身が認めている）。

もっと本質的な反論: **0円の段階で検索ボリュームは制約になっていない。**

売上が出ない原因は「やや検索数の少ないニッチを選んだ」ことではなく、
(a) 換金経路が無い (b) 公開していない、のどちらかである。
ボリュームが効くのは「有望ニッチが20個あってどれを選ぶか」という状況で、
それは3ヶ月目の問題であって1週目の問題ではない。

**対案**: `production_fit` に「自前の `AFF_*` で換金経路が組めるか」の実測を入れた
（`Scorer._has_route`）。これは**今すぐ・無料・確実**に測れて、しかも (a) を直接潰す。
有料の検索需要APIは、広告出稿実績を要求しない従量課金型（DataForSEO 等）を
**初売上の資金で**入れる。順序を逆にしない。

### ① 条件付き Gemini は、おそらく永久に不要

GPT の条件設定（根拠が少ない / 矛盾している / now-watch境界）は妥当。
ただし **その条件が発火する場面こそ、④⑤で実データを入れるべき場面**である。

「競合が多いか少ないか」で意見が割れているとき、必要なのは**2つ目のLLMの意見**ではなく
**1つ目の実測データ**。SERP を実測すれば意見の相違そのものが消える。
LLM を2つ並べて多数決を取るのは、どちらも推測である限り精度が上がらない。

**同じ複雑さ予算を使うなら、Gemini より SERP API に使う。** これが実装者の判断。
（インターフェースは `SerpAnalyzer(provider=...)` で開けてあるので、
本当に必要になったら差し込める）

### ⑩ EV式を今スコアに使うのは反対（フレームは採用）

```
EV = P(traffic) × P(click) × P(CV) × commission − cost
```

**この4つの確率は N=0 では推定できない。** 推定できない数値を4つ掛けると、
「精密そうに見える捏造」が出てくる。これは②で今まさに直した「占い」問題の再発で、
しかも小数点付きなので粗いスコアより悪い（人間が信じてしまう）。

**対案 — EV を「予測」ではなく「実測KPI」として先に立てる:**

| 項 | 今できること |
|---|---|
| 分母（人間の注意） | **実測できる。** 承認・採用のたびに秒数を積算（`ExperimentLedger.record_attention`） |
| 分子（収益） | **実測できる。** `/metrics` の revenue |
| P(traffic/click/CV) | **推定しない。** `/metrics` の実績から後で回帰する |

つまり `判断1分あたり収益` を**今日から実測して表示する**。予測式は Ledger に
20件溜まってから当てはめる。

さらに実装者として言い添えたいこと: **N=0 における EV の最大のレバーは順位精度ではない。**
週1採用・1日5分の運用では、ランキングを 10% 改善しても絶対値はほぼ動かない。
効くのは「判断1回あたりの秒数を削る」か「公開率を上げる」方。
だから Ledger は収益より先に**注意時間**を可視化している。

## 実装したもの（新規API依存ゼロ）

```
src/scout/evidence.py   観測と推測の分離。observed が inferred を置き換える
                        mapping_version で換算ルールの版を記録（後から校正するため）
src/scout/serp.py       SERPの守備力。provider差し替え式
                        heuristic(無料/代理/confidence 0.45固定) | dataforseo | serpapi
src/scout/explore.py    explore/exploit 予算。採用済みと語が重なるかで振り分け
src/scout/funnel.py     Stage1〜5 の切り分けと、試行回数ベースの撤退判定
src/scout/ledger.py     予測の凍結・実績追記・注意時間の積算・校正ゲート
```

`Score` は素点100点を保ったまま、`discovery` / `business` / `opportunity` を並列で持つ
（GPT提案⑥）。`llm_total`（推測のみ）と `total`（実測込み）も両方保持するので、
「実測導入で何点動いたか」が後から検証できる。

## スコアの構造

```
opportunity = √(discovery × business)          ← 最終順位

discovery = momentum × whitespace × confidence
            成長性     競合の空き   根拠の信頼度
            ↑x_velocity ↑SERP分類   ↑実測で埋まった軸の割合で上がる

business  = demand × monetization × production_fit
            需要      収益化       制作相性
                     ↑収益性+アフィリ相性  ↑AFF_*で換金経路が組めるか(実測)
```

`confidence` を discovery に掛けているので、**根拠が薄い候補は自動的に順位が下がる**。
実測が1つも無い初期は全候補が等しく割り引かれるため、順位自体は歪まない。

## 残っている弱点（正直に）

1. **SERP は代理指標**。Google の実際の順位ではなく `web_search` の結果を分類している。
   `confidence` を 0.45 に固定して「実測を騙らない」ようにしてあるが、本物ではない
2. **検索ボリュームは未実測**。上記の理由で初売上後に回した
3. **`/metrics` は手入力**。週1回30秒だが、忘れると Ledger が育たない
4. **換算ルール（observed→点数）は人間の仮説**。`mapping_version` を打ってあるだけで、
   まだ校正されていない。ここは Ledger が20件溜まるまで動かせない

## 次の優先順位（実装者版）

GPT の順位と違う部分がある。理由は「初売上までの距離」で並べ替えたため。

| 優先 | 項目 | GPTの順位 | 変更理由 |
|---|---|---|---|
| 1 | **`/metrics` を実際に週1で入れる運用を回す** | — | Ledger が育たないと以降すべて動かない |
| 2 | 採用ニッチ専用のアフィリ案件を増やす | — | `production_fit` を上げるのが最短で売上に効く |
| 3 | SERP を有料APIに差し替え | 2位 | 同意。ただし初売上の資金で |
| 4 | 検索需要の実測（従量課金API） | 1位 | **4位に落とした**（上記⑤の理由） |
| 5 | 予測 vs 実績の回帰と配点校正 | 6位 | Ledger 20件が前提。時期が来たら |
| 6 | 条件付き Gemini | 7位 | **おそらく不要**（上記①の理由） |

---

# 第3ラウンド — 収束。confidence の扱いと /metrics 運用を修正

GPT が反論3点（検索需要APIの優先度 / Gemini不要 / EV式を予測に使わない）をすべて受け入れ、
代わりに**設計上の重要なバグを1つ指摘してきた**。それが正しかったので直した。

## GPT の指摘（採用）: confidence を Discovery に掛けてはいけない

第2ラウンドの実装はこうだった。

```
discovery = 成長性 × 競合の空き × confidence
```

一見もっともらしいが、**早期発見システムとして逆に働く**。

本当に早いトレンドほど、ソースが少なく、観測回数も少なく、SERP がまだ形成されていない。
つまり **早い → 根拠が薄い → confidence が低い → 順位が落ちる**。
これでは成熟したネタを好むシステムになる。目的と真逆。

修正:

```
discovery = 成長性 × 競合の空き        ← confidence は掛けない
confidence = 0〜1                      ← 不確実性として別に持つ
```

そのうえで confidence の使いどころを3つに分けた。

| 用途 | 扱い |
|---|---|
| **順位** | 使わない（上記の理由） |
| **表示** | 「機会スコア 82 / 確信度 0.31」と並べる。「82点だが不確実」と人間が読める |
| **`now` 判定のゲート** | 使う。低確信で自動的に本気の投資はさせず watch に留める |
| **explore 枠** | 「高スコア × 低確信」を🎲マークで明示し、上位に1件繰り上げる |

4つ目は実装者側の追加。GPT は「explore 枠では高Opportunity+低Confidenceを意図的に試す
価値がある」と書いていたが、ランキングに任せると埋もれる。明示的に繰り上げないと
「確信が高いものだけ試す」に収束して学習が進まない。

## その他の採用点

### いいね/時間を log 変換にした（線形をやめた）

SNS 指標の分布は 10 / 20 / 40 / 80 / 160 / 1000 / 10000 のように極端に歪む。
「閾値の4倍で満点」の線形換算だと上位が飽和し下位が過大評価される。

```
旧: ratio = velocity / (閾値 × 4)              線形
新: ratio = log1p(velocity) / log1p(閾値 × 20)  対数
```

最終的には同一プラットフォーム・カテゴリ内の percentile にしたい
（上位10% → 15点、中央値 → 7点）。分布が取れるまでは log で近似する。
SERP weakness は既に 0-100 に正規化された複合指標なので線形のままでよい。

### SERP に特徴量を3つ追加

| 特徴量 | 意味 | 補正 |
|---|---|---|
| **Intent Fragmentation** | 上位が解説/商品/掲示板/ニュースで混在 = Google も決定版を持っていない | +12 |
| **Answer Coverage** | 「〜方法」「比較」等が無く「〜とは」「公式」ばかり = 答えが供給されていない | +12（低いとき） |
| **Domain Concentration** | 同一ドメインが枠を占有。強者独占とも「他に無い」とも読めるので補助扱い | -5（小さく） |

ホスト名で「弱い」と固定しない方針も採用した（note にも強い記事はある）。
ページ種別は特徴量の1つに留めている。

### Stage 判定をプラットフォーム別 + 自アカウント baseline に

絶対値のベンチマークを信用しすぎない。判定の基準はこの順でフォールバックする。

1. **自アカウントの実績中央値**（10件以上溜まったら。中央値 × 0.3 を配信失敗ライン）
2. **プラットフォーム既定**（TikTok / YouTube Shorts / X で別。Shorts はサムネ表示と
   実視聴の差が大きいので1投稿あたり下限を高く、反応率下限を低く）
3. 絶対値の仮値

### 最重要KPI を2つ立てた

GPT の提案「初売上までに何件 adopt したか」を採用。ただし**一度しか観測できない遅行指標**
なので、毎週見られる先行指標を並べた。

- **初売上までの採用件数**（3件なら探索が効いている / 20件超で0円なら根本的に間違い）
- **公開到達率**（採用したニッチのうち1本以上公開できた割合）
  ← 採用しても公開に至っていなければ、下流のスコア精度をいくら上げても意味がない

### explore 比率を固定せず状態から決める

```
winner 0件 → 100% explore   （exploit 対象がそもそも無い）
      1件 →  30%
      3件 →  20%
      5件 →  15%
```

## 最重要課題だった `/metrics` を4段構えで潰した

「人間に全数字を入力させない」が GPT の回答で、これが正しい。

| 段 | 対策 | 実装 |
|---|---|---|
| 1 | **自前で分かるものは自動入力** | `posts` は `data/posts.csv` から自動集計。人間に数えさせない |
| 2 | **入力は累計値でよい** | 差分はシステムが計算する。人間に引き算をさせない |
| 3 | **必須は金側だけ** | `/m <niche> 3200` の1数字でも受け付ける |
| 4 | **入れなくても止まらない** | Stage 0（判定不能）として記録し UNKNOWN のまま前に進む |

入力形式は個数で意味が決まるので曖昧にならない。

```
/m xxx 3200                  → revenue
/m xxx 6000 3200             → views, revenue
/m xxx 6000 45 1 3200        → views, clicks, conversions, revenue
/m xxx views=6000 revenue=3200  → 名前付き（順不同・混在可）
```

**Core / Diagnostic の分離も入れた。**

- Core（Stage 判定に必要）: `posts` / `views` / `revenue`
- Diagnostic（取れたときだけ）: `engaged` / `clicks` / `conversions`

実装者側の追加として **Stage 0（判定不能）を明示**した。views が未入力のとき
Stage 1（配信されない = ニッチが悪い）と Stage 4（売れない = 案件が悪い）は区別できない。
区別できないものを Stage 1 と診断すると、**良いニッチを誤って捨てる**。
「取れていない」を「悪い」と読み替えないのは、Diagnostic 全体に適用した原則。

さらに `weekly-metrics.yml` を追加した。未更新の採用ニッチだけを、
そのまま数字を書き換えてコメントできる形で週1回 Issue に出す。

## 延期を確定させたもの

| 項目 | 状態 |
|---|---|
| Gemini | **入れない。** 「念のため」はやらない。実測上の理由が出たときだけ |
| Google Ads API | **入れない。** Explorer Access では Planning 系が制限され、Keyword Research には Basic/Standard が必要。初売上後に従量課金APIを検討 |
| 予測 EV 式 | **作らない。** 実測KPI（判断1分あたり収益）のみ |
| YouTube Data API の公開統計 | 前提が足りないため延期。自動取得には投稿時に動画IDを保存する必要があり、現状の publisher は TikTok と記事のみ |

## 現在の優先順位（GPT と合意）

1. `/metrics` 入力を極限まで減らす ← **実装済**
2. `/adopt` → publish → revenue を1周回す ← **次はここ。コードではなく運用**
3. `production_fit` = 換金経路を増やす
4. confidence をスコアから分離 ← **実装済**
5. platform 別 Stage 判定 ← **実装済**
6. SERP Weakness 特徴量追加 ← **実装済**
7. 初売上
8. SERP API（有料）
9. Search Volume（有料）
10. 20〜50件で score calibration

**このシステムの現在地**: 「LLM が稼げそうだと思うネタ探索機」。
教師データが20〜50件溜まった時点で「自分自身の実績から稼げる確率を学習する探索機」に
進化できる。そこまでの最短距離は、ランキング精度ではなく
**Discovery → Adopt → Publish → Revenue を何周回せるか**。

---

# 第4ラウンド（設計凍結） — 6点の判定と CHEAP_TEST の実装

GPT の最終レビュー6点を「① 今すぐ修正すべき設計バグ / ② ルールだけ明文化 /
③ データが溜まるまで延期」に分類した。**①に入れた4点は実装済み。**

| # | 指摘 | 判定 | 対応 |
|---|---|---|---|
| 1 | confidence 低 → watch のゲートは早期発見と衝突している。commitment level を分離したい | **① バグ** | `src/scout/commitment.py` を新設。実装済み |
| 2 | speculative の 30 / 0.45 は未校正の探索仮説として扱いたい | **② 明文化＋軽微修正** | version 定数化 + 選定を相対順位に変更 |
| 3 | Stage 1 を「ニッチ失敗」と断定しない | **① バグ** | `DISTRIBUTION_FAILURE` に改名し原因推定を分離。実装済み |
| 4 | production_fit が既存 AFF 案件へ過学習する | **② 構造だけ** | 3値化（immediate / potential / none）。実装済み |
| 5 | 最重要KPI を3層に | **① バグ寄り** | Execution / Speed to Signal / Economics を実装済み |
| 6 | 20件までは配点を変えない。ただし5件ごとに異常検知はする | **① 実装** | `diagnostic_review()` を実装済み |

## ①-1 CHEAP_TEST は入れるべきだった（GPT の見立てに同意）

指摘が正しい。confidence を順位から外しても、`now` のゲートに使う限り
**「早い → データが少ない → confidence 低 → 実行されない」が残っていた。**
`speculative` フラグはレポートに印を付けるだけで、実際に賭ける手段が無かった。

**ただしラベルだけでは意味がない。** 「CHEAP_TEST です」と表示するだけでは、
人間が結局フル運用するか放置するかの二択になる。なので投資レベルに
**生成枠（本数上限）と自動遷移** を持たせた。

| レベル | 1回の生成で作る本数 | 公開上限 | 意味 |
|---|---|---|---|
| OBSERVE | 0 | — | 監視のみ |
| **CHEAP_TEST** | **1** | **3** | 小さく試して実データを取る |
| ADOPT | 2 | 上限なし | 通常運用 |
| SCALE | 4 | 上限なし | 実績が出たので増やす |
| EXIT | 0 | — | 撤退 |

生成枠は `NicheRegistry.item_caps()` → `Pipeline._apply_item_caps()` で実際に効く。
CHEAP_TEST のニッチから大量に作られたら「小さく試す」が成立しないため。

自動遷移（実測ベース。推測では動かさない）:

```
売上が出た                        → SCALE
Stage 0 / 試行不足                → 変更しない
Stage 1 かつ 切り口 < 2種類        → CHEAP_TEST のまま（切り口を変えて再試行）
Stage 1 かつ 切り口 >= 2種類       → EXIT
Stage 2〜4（配信は成立している）    → ADOPT に昇格して原因を調べる
```

コマンドは `/test`（CHEAP_TEST）/ `/adopt`（自動判定）/ `/scale` / `/drop`。
`/adopt` は確信度から自動でレベルを決めるので、**確信が低い候補は自動的に
CHEAP_TEST になる**（watch に落ちない）。

> 命名について1点。GPT の ladder は `ADOPT` を投資レベル名にしているが、
> こちらは `/adopt` がコマンド名なので衝突する。コマンドは維持し、
> レベル名としての ADOPT は「通常運用」の意味に固定した。

## ②-2 speculative しきい値は「未校正の探索仮説」として明示

`SPECULATIVE_RULE_VERSION = "2026-08-19.1"` を定数化し、Ledger の予測行に記録した。
percentile / Pareto frontier への移行は延期（GPT の判断に同意）。

**ただし、しきい値への依存だけは今のうちに薄くした。** 固定値の唯一の実質的な帰結は
「どの候補を上位に繰り上げるか」なので、そこを**候補集合内の相対順位**に変えた。

```
機会スコアの percentile − 確信度の percentile が最大の候補を1件繰り上げる
```

これなら 30 / 0.45 が多少ズレていても選択は壊れない。しきい値はラベル付けだけに使う。

## ①-3 Stage 1 は症状、原因は別に持つ

指摘のとおり、Stage 1 を「ニッチ失敗」と読むと**良いニッチを捨てる**。
10投稿で平均100 views の原因には、ニッチ・フック・構成・投稿時間・アカウント状態・
プラットフォーム相性・クリエイティブ品質が混ざっている。

- Stage 1 の名前を **「配信の失敗」**（DISTRIBUTION_FAILURE）に変更
- `FunnelVerdict.likely_cause` を分離（unknown / creative / niche / funnel / offer）
- **ニッチ撤退の推奨には比較材料を要求する**

```
切り口 < 2種類                         → cause=creative（撤退させない）
切り口 >= 2種類 かつ 他ニッチ中央値の30%未満 → cause=niche（撤退を提案）
切り口 >= 2種類 かつ 他ニッチも同程度に低い  → cause=creative（制作側を疑う）
```

`should_exit` は `likely_cause == niche` のときだけ True になる。
「Aニッチ 3000 / Bニッチ 200」という比較ができて初めてニッチ原因と言う。

## ②-4 production_fit を3値にした（過学習の構造的回避）

`route_available: bool` を `monetization_readiness` の3値に置き換えた。

| 値 | 判定方法 | production_fit の重み |
|---|---|---|
| `immediate` | 自前の `AFF_*` に案件がある（実測） | 1.0 |
| `potential` | 案件は無いが調査結果に具体的な収益化手段・商品が挙がっている | 0.6 |
| `none` | 換金の道が見えない | **0.2（ゼロにはしない）** |

`potential` の判定は**既に集めている調査結果**（`monetization_paths` / `best_product`）
から行うので、LLM 呼び出しは増えていない。

`none` を 0 にしないのが要点。「AFF_* が無い = 金にならない」と学習させると、
すでに案件を持っている市場ばかり探索するシステムになる。案件は後から開拓できる。

## ①-5 最重要KPI を3層に

North Star（初売上までの採用件数）は維持。単独だと「1採用で100円」が過大評価されるので、
診断用に3層を並べた。

```
1. Execution        公開到達率 = 1本以上公開できた採用 / 全採用
2. Speed to Signal  初売上までに市場へ出した本数
3. Economics        公開1本あたり収益 / 1,000viewsあたり収益
```

探索できたか → 公開できたか → 反応されたか → 金になったか、を分離する。

## ①-6 5件ごとの診断レビュー（配点は変えない）

統計的校正（20件以降）と運用上の異常検知は別物。N=5 でも「5採用して公開0本」は
明らかに問題で、それは配点の話ではない。`diagnostic_review()` が採用5件ごとに出す。

- 公開到達率（0本なら 🚨）
- Stage 分布
- 推定原因の偏り（同じ原因が3件以上集中していたら警告）
- speculative と通常の収益化率の差（speculative が3件連続で外れたらしきい値を疑う）
- views 未入力率（半分以上なら台帳が育たない警告）

**配点・mapping・ランキング式は一切変更しない**ことを出力にも明記している。

## 延期を確定（GPT の判断に同意）

Gemini / Google Ads API / DataForSEO / SERP API / 予測EV / 機械学習 / bandit / DB /
Dashboard、および speculative しきい値の高度化・monetization_potential のスコア化・
percentile 化・score calibration。**実際にボトルネックになってから追加する。**

## 設計凍結

これで設計は凍結。GPT の「最終的に欲しい状態」（候補が CHEAP_TEST → ADOPT → SCALE /
EXIT へ自動遷移する）は実装済みで、あとは実データが流れれば動く。

残る弱点はすべて「データがまだ無い」ことに起因し、コードでは解決できない。

1. SERP は代理指標（実SERPは有料。初売上後）
2. 検索ボリュームは未実測（同上）
3. observed→点数の換算ルールは未校正（Ledger 20件で）
4. 自アカウント baseline / peer 比較はサンプル0件（実績が溜まれば有効化）

**次のフェーズはコードを書くことではなく、Discovery → Test → Adopt → Publish → Revenue
を回してデータを作ること。**

---

# 第5ラウンド（凍結） — 昇格に3つの歯止めを入れた

最終確認の3点はすべて妥当だった。特に1番は**実運用前に直すべきバグ**だったので実装した。

## ① 1件の売上で SCALE しない（「売れた」と「再現性がある」を分ける）

旧実装は `revenue > 0 → SCALE` で、**初回の成功シグナルに過剰反応していた。**
1件の売上は偶然の可能性があるのに、生成枠を1本から4本に増やしていた。

修正後の判定（`is_reproducible`）:

| 状態 | 遷移 |
|---|---|
| 初回売上（CV 1件 / 売上観測1回） | **ADOPT**（昇格はするが SCALE にしない） |
| CV 2件以上 | **SCALE** |
| 売上が増えたことを2回以上観測 | **SCALE** |

CV が取れないプラットフォームでも判定できるよう、条件を2本立てにした。
「売上が増えた回数」は Ledger の履歴から数える（`ExperimentLedger.revenue_events`）。
閾値は `config.yaml` の `scout.scale_gate` で変更できる仮値。

## ② Stage 2〜4 を一律 ADOPT にしない（診断中フラグ）

指摘のとおり「配信が成立している」と「制作量を増やすべき」は別。Stage 4 の状態で
生成枠を増やすと、**悪い導線に対して制作量だけ増える。**

新しいレベル名は増やさず、`Niche.diagnosing` を持たせた。

```
Stage 2 → diagnosing = "creative"   切り口が未解消
Stage 3 → diagnosing = "funnel"     導線・CTA が未解消
Stage 4 → diagnosing = "offer"      案件・商品 が未解消
```

診断中は**レベルを昇格させず、生成枠を1本に抑える**（`budget_for(level, diagnosing)`）。
ADOPT でも SCALE でも診断中は1本。売上が出るか Stage が改善すればフラグは解除される。
ニッチ撤退はしない（配信は成立しているので）点は従来どおり。

## ③ potential は inferred として保存する

`monetization_readiness` の3値は派生値にして、根拠を種類ごとに分けて保存した。

| フィールド | 種類 | 判定方法 |
|---|---|---|
| `monetization_observed` | **実測** | `AFF_*` に案件が実在する |
| `monetization_inferred` | **推論** | 調査結果に収益化手段・商品が挙がっている |

`monetization_readiness`（immediate / potential / none）と
`monetization_source`（observed / inferred / none）はどちらも派生値。
Ledger の予測行と校正表に `monetization_source` を記録するので、
**「LLM が稼げると言った」と「実際に案件が存在した」を同じ種類のデータとして扱わない。**

`potential` に 0.6 を与えるのは初期仮説のままだが、校正時に両者を分離できる。
旧形式（`monetization_readiness` / `route_available`）の台帳も読めるようにしてある。

## 実装中に見つけた別のバグ

`/adopt` を明示的に叩いたのに、スコアが低い候補が `OBSERVE`（何もしない）に落ちていた。
**人間の明示指示が無視される**状態だったので、`/adopt` の下限を `CHEAP_TEST` にした。
OBSERVE は自動分類のためのレベルで、明示指示には使わない。

## 設計凍結

これで凍結。実装としてやることは尽きた。テスト 140 件通過。

残る弱点はすべて「データがまだ無い」ことに起因し、コードでは解決できない。

1. SERP は代理指標（実SERPは有料。初売上後）
2. 検索ボリュームは未実測（同上）
3. observed→点数の換算ルールは未校正（Ledger 20件で）
4. 自アカウント baseline / 他ニッチ比較はサンプル0件（実績が溜まれば自動で有効化）
5. `scale_gate` の閾値（CV 2件 / 売上2回）も仮値（同上）

**次はコードを書くフェーズではなく、Discovery → Test → Adopt → Publish → Revenue を
回してデータを作るフェーズ。**

---

# 第6ラウンド（凍結確定） — 再開条件を3つに固定

GPT が再開条件を1つ追加した。妥当なので実装した。

## 追加された再開条件: 5件診断で同じ異常が2回連続

「最初の5件で公開到達率10%、次の5件でも15%」のようなケースは、20件を待たずに点検すべき。
ただし**点検の対象は実装バグ・運用障害に限り、アルゴリズムは触らない。**

実装上の問題が1つあった。**前回のレビュー結果を保存していないと「2回連続」を判定できない。**
なので診断レビューを構造化した。

1. `detect_anomalies()` が異常を**コード**で返す（`publish_zero` / `metrics_missing` /
   `stage1_dominant` / `cause_concentrated` / `speculative_shutout` /
   `diagnosing_concentrated` / `transition_stuck`）
2. レビュー実行時に結果を `kind: "review"` として台帳に追記する
3. `repeated_anomalies()` が過去のレビューと突き合わせて連続回数を出す
4. 出力を2つに分ける

```
**今すぐ直してよい（実装バグ・運用障害）**
- 🚨 2回連続 採用しても1本も公開されていない: 10件採用して公開0本

**記録のみ（アルゴリズムは20件まで触らない）**
- ・推定原因が1つに集中している: `offer` に 3件
```

2回連続したときは、触ってよい範囲を出力に明記する。

> 点検してよいのは実装バグ・運用障害だけです（公開されない / metrics が取れない /
> 状態遷移がおかしい / 人間の明示指示が無視される）。
> 配点・mapping・scale_gate・speculative閾値・opportunity計算式・monetization の重みは
> 触りません。

## 初売上の振り返りを実装した

「初売上が出てもすぐ配点を変えない。まず事実を記録して再現性を見る」に対応して、
週次レポートに自動で出る振り返りを作った。GPT が挙げた5点がそのまま埋まる。

```
### 🎉 初売上の振り返り（配点はまだ変えない）

- 何が売れたか: インボイス2割特例の終了（`n_w`）
- 最初に作ったもの: 会計ソフト比較記事
- 通った Stage: Stage1 → Stage4 → Stage5
- 候補の種類: 探索候補（speculative）
- 採用時の投資レベル: CHEAP_TEST
- 換金の根拠: inferred（potential）
- 採用時の予測: 機会 58.1 / 確信 0.3
- 到達までの採用件数: 2件 / 公開本数: 8本

次にやるのは配点変更ではなく再現性の確認。
```

`Stage1 → Stage4 → Stage5` のような遷移履歴が残るので、
「配信で1回つまずいたが切り口を変えたら売れた」という経路が後から読める。

## 運用フェーズの契約を文書化した

再開条件と変更禁止範囲を [OPERATIONS.md](OPERATIONS.md) に固定した。
迷ったときにコードではなくこの文書を読む形にしてある。

- アルゴリズムを触ってよい3条件
- 20件未満でも直してよいもの（実装バグ・運用障害）
- 20件未満では原則触らないもの（配点 / mapping / scale_gate / speculative閾値 /
  opportunity計算式 / monetization の重み / percentile化 / 回帰 / 機械学習）
- 毎日・毎週の手順
- 初売上が出たときの手順
- 20件で0円だったときの点検順序

## 凍結確定

**設計凍結。テスト 140+ 件通過。次はデータを作るフェーズ。**

---

# 第7ラウンド（運用開始後） — LLM を Gemini 無料枠に差し替えた

**設計の変更ではない。** LLM プロバイダは `FROZEN_UNTIL_CALIBRATION` に含まれない
（配点 / mapping / scale_gate / speculative閾値 / opportunity計算式 /
monetization重み / percentile化 / 回帰 / 機械学習 のいずれでもない）ので、
凍結は崩していない。

## 差し替えた理由：ランニングコスト

運用開始時に前提が2つ崩れた。

| | 想定 | 実際 |
|---|---|---|
| X API | 無料枠で読める | **無料枠は新規提供停止。** 従量課金で月$30前後 |
| Claude API | 月500〜2,000円 | **既定の opus-5 + web_search で月$30〜60** |

合計 月$60〜90。**売上0円の段階で出す額ではない。**
オーナーの判断は「課金しない」だったので、**無料枠があるのは Gemini だけ**という
理由で Gemini に差し替えた。

OpenAI（gpt-5-mini で月$4程度）も検討したが、課金しない方針のため見送り。

## 実装

```
src/llm/gemini.py          Gemini アダプタ（新規）
src/llm/claude.py          そのまま残す（初売上後に戻せるように）
config.yaml  llm.provider  'gemini' | 'claude'
```

呼び出し側は変更なし。LLM のインターフェースを
`available` / `generate_json` / `research` の3つに閉じておいたので、
アダプタ1ファイルの追加で済んだ。

### Gemini 固有の対応

| 問題 | 対応 |
|---|---|
| `responseSchema` が JSON Schema の全キーワードを受け付けない（`additionalProperties` / `minimum` / `maximum` は非対応で 400 になる） | `_sanitize_schema` で再帰的に削る。**元のスキーマは壊さない**（Claude 側でも使うため） |
| Google 検索グラウンディングと `responseSchema` の併用が保証されていない | 既存設計どおり「検索して読む」と「構造化する」を2回に分ける。Claude 側と同じ呼び出し形になった |
| 無料枠のレート制限 | 429 を検出したら「`research_limit` を下げてください」と警告して None を返す。パイプラインは止まらない |
| 安全性ブロック | `promptFeedback.blockReason` を検出してログに出し、空文字を返す |

### 副作用: SERP 代理指標が実 Google に近づいた

`research()` の参照 URL は SERP 代理指標（`src/scout/serp.py`）の入力になる。
Claude の web_search から **Google 検索グラウンディング** に変わったので、
分類対象が Google のインデックスに一段近づいた。

第2ラウンドで「Gemini は入れない」と決めたが、あのときの論点は
「2つ目のLLMの意見は要らない、必要なのは実測データ」だった。今回は
**コストが理由で、しかも実測データの質が上がる**ので、結論が変わっている。
判断を覆したのではなく、前提が変わった。

`confidence` は 0.45 に固定したまま据え置く。グラウンディングでも
「Google の実際の順位」ではないので、実測を騙らせない方針は維持する。

### プロバイダを台帳に記録するようにした

プロバイダを変えると `inferred` スコアの分布が変わるので、校正時に
Claude 期と Gemini 期の予測を同じデータとして扱えない。
`mapping_version` と同じ発想で、予測行に `llm_provider` / `llm_model` を記録した。
`calibrate` の出力にも `llm` 列が出る。

## 運用フェーズで直した実装バグ（台帳）

運用を始めてから見つかった順に記録する。**すべて `OPERATIONS.md` §2（実装バグ・
運用障害）であり、§3 の凍結対象には一度も触っていない。** 発見の経緯まで書くのは、
どこを見れば次のバグが見つかるかが経緯に出るため。

| # | 症状 | 原因 | 見つけ方 |
|---|---|---|---|
| 1 | `daily-generate` が 401 で全滅 | `--sample` が `store_true` で未指定時に `False`。トークン無し自動判定が死んだ | オーナーへの失敗メール |
| 2 | 実行ごとに状態ファイルが汚れる | 承認キューに絶対パスを保存（runner と手元で違う） | コミット前の差分 |
| 3 | Gemini が 404 で全滅 | モデル名が新規ユーザーに提供停止。API 自身が後継名を返していた | Actions ログ |
| 4 | ログがキーワードごとの警告で埋まる | 換金経路の有無を調べるだけの呼び出しでも警告していた | Actions ログ |
| 5 | 候補が「未採点」に落ちる | 429/503 にリトライが無く、裏取り成功後の採点だけ捨てていた | Actions ログ |
| 6 | `monetization_observed` が常に False | `daily-scout.yml` に `AFF_*` を渡し忘れ。制作側には渡してあった | **オーナーの指摘**（「登録済みと出る」） |
| 7 | レポートで承認処理が誤発火しうる | 週次レポート Issue が `approval-queue` ラベルだった | 未実行ワークフローの試運転 |
| 8 | 採用0件でも「承認が止まっている」 | 「止まった」と「まだ始まっていない」を区別していなかった | 同上 |
| 9 | 承認画面が生成元を誤表示 | `generated_by` に `"claude"` を固定。テンプレ落ちの案内も `ANTHROPIC_API_KEY` 固定 | 過去ログの読み直し |
| 10 | その回の状態が失われうる | `daily-scout` / `daily-generate` が pull せずに push | ワークフローの読み合わせ |
| 11 | `max_duration_sec` が効かない | 台本の指示に下限しか書いておらず、builder は超過を警告するだけ | Gemini 生成の初成功ログ |
| 12 | 実データを1件も読まないまま毎晩動いていた | `X_BEARER_TOKEN` / `XAI_API_KEY` が未登録。探索は固定サンプル候補2件、制作は固定サンプルツイート4件に落ちていた。探索側は警告を出していたが**マーカーが無く**、制作側は自動判定の経路だけ**警告すら出していなかった** | オーナーの「ツイート読み込んで学習して」から状態を確認 |

### 経緯に出ているパターン

**12件のうち 5件（#1 / #6 / #9 / #11 / #12）は「探索側と制作側で設定が非対称」だった。**
2レイヤ構成にした結果、片側だけ直して反対側を忘れる形のバグが繰り返し出ている。

- #1 探索側にはトークン無しのガードがあり、制作側に無かった
- #6 制作側には `AFF_*` を渡してあり、探索側に無かった
- #9 探索側は `llm_provider` を台帳に記録しており、制作側は捨てていた
- #11 builder は上限を知っているが、台本を書く側に伝えていなかった
- #12 探索側はサンプルに落ちたことを警告していたが、制作側の自動判定経路は黙っていた

そのため #6 以降は、**同種の取り違えを落とすテストを対称性そのものに対して書く**
方針にした（`config.yaml` から必要スロットを導出して3ワークフロー全部を検証する、
全プロバイダが `api_key_env` を持つことを検証する、状態をコミットするワークフローが
push 前に pull することを検証する、など）。個別の値ではなく「片側だけ直っている」
状態を検出させる。

**#3 / #5 は外部 API 側の変化**で、コードの誤りではない。モデル名の提供停止と
無料枠のレート制限はこれからも起きるので、どちらも「オーナーが直すべき場所を
名指しする警告」を出すようにした。#9 を直した理由も同じで、案内が間違っていると
オーナーは直せない場所を確認しにいくことになる。

### 参考: #1 の詳細

`daily-generate` が schedule 実行で 401 全滅していた（2026-08-22）。
`OPERATIONS.md` §2 の「実装バグ」に該当するため、20件を待たずに修正。

原因: `--sample` を argparse の `store_true` で定義していたため、未指定時に
`None` ではなく `False` が渡り、「トークンが無ければサンプルに落とす」自動判定
（`use_sample is None` の分岐）が働かず、空の `X_BEARER_TOKEN` で X API を叩いていた。

探索側が通っていたのは `XApiSource.available` がトークンの有無で発掘元ごと
スキップしていたため。**制作側の収集にだけ同等のガードが無かった**という非対称が原因。

修正後は `daily-generate` が success。回帰テスト2件を追加した。
