# --- Core ---
requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0

# --- 原稿生成 (Claude) ---
anthropic>=0.40

# --- Video generation ---
Pillow>=10.0        # スライド画像生成
gTTS>=2.5           # 音声合成 (オンライン, 無料). 代替: pyttsx3 (オフライン)
# ffmpeg はOSパッケージで別途インストールが必要 (subprocess から利用)
# 日本語フォントも必要: Ubuntu `apt install fonts-noto-cjk`

# --- Dev ---
pytest>=8.0
